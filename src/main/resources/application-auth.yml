safehome:
  jwt:
    secret: ${JWT_SECRET:localdevkey-safehome-must-be-at-least-32-bytes}
    access-token-expiry: 3600      # 1시간 (초)
    refresh-token-expiry: 1209600  # 14일 (초)
  kakao:
    admin-key: ${KAKAO_ADMIN_KEY:}
