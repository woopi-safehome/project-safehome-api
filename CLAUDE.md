# CLAUDE.md — project-safehome-api

**이 저장소에서의 역할과, 어겼을 때 조용히 깨지는 것들.**
구조·실행 방법·계약 스펙 → [`README.md`](README.md)

---

## 역할과 경계

서버다. 데이터 저장·조회, 비즈니스 로직, 인증을 맡는다.
클라이언트의 **유일한 통신 대상**이고, AI 분석 서버를 호출하는 **유일한 주체**다.

**여기서 하지 않는 일**

- **AI 분석을 하지 않는다.** 프롬프트·판정 기준은 AI 분석 서버 소관이다. 호출하고 결과를 통과시킬 뿐이다.
- **화면을 만들지 않는다.** 응답은 JSON까지다.

**혼자 정하지 않는 것** — 아래는 확인을 받고 진행한다.

- 클라이언트로 나가는 응답 형식의 변경 (계약이다)
- 되돌리기 어려운 스키마 변경
- 참조용으로 존재하는 코드의 제거

## 시작 전

1. [`README.md`](README.md) — 구조 · 작업 레시피 · **App→API 계약**
2. 건드릴 도메인의 `README.md` — 그 도메인의 규칙·상태·설계 이유

## 필수 절차

- **조회에는 읽기 전용 트랜잭션을 명시한다.** 트랜잭션 애노테이션의 읽기 여부가 읽기/쓰기 커넥션 선택을 결정한다.
  빠뜨려도 **동작은 정상이라 드러나지 않는다** — 조회가 조용히 쓰기 커넥션을 쓴다.
- **테이블을 바꾸면 스키마 파일을 함께 고친다.** 자동 DDL이 꺼져 있어 엔티티만 바꿔도 테이블은 따라오지 않는다.
- **응답 형식을 바꾸면 계약 절을 같은 커밋에서 갱신**하고 하류까지 전파한다.
  필드 추가는 안전하지만 **제거·개명은 이미 배포된 클라이언트를 깨뜨린다.**
- **분석 결과는 캐시된다.** 상류의 프롬프트나 판정 로직이 바뀌면 **캐시 키의 버전을 올려야 한다.**
  올리지 않으면 옛 결과가 만료될 때까지 계속 나간다.
- 새 도메인은 구조를 고안하지 말고 README의 도메인 구조를 그대로 따르며, **도메인 README를 함께 만든다.**

## 금지사항

- **인증을 프레임워크가 해준다고 가정하지 않는다.** 보안 필터도 인터셉터도 없다.
  인자 리졸버가 인증 헤더를 직접 해석하는 것이 **유일한 인증 지점**이다.
  보호가 필요한 엔드포인트가 사용자 식별자를 인자로 받지 않으면 **아무 검사 없이 열린다.**
- **에러 응답을 직접 만들지 않는다.** 정해진 예외를 던지면 전역 핸들러가 변환한다.
  컨트롤러에서 try/catch로 삼키면 상태 코드와 에러 코드가 어긋난다.
- **응답을 공통 봉투 없이 내보내지 않는다.** 단 스트리밍 응답은 예외이며, 이벤트를 봉투 없이 그대로 보낸다.
- **데이터소스 컨텍스트를 직접 조작하지 않는다.** 트랜잭션 애노테이션만 정확히 붙인다.
- **도메인 모델에 영속성 애노테이션을 붙이지 않는다.** 엔티티는 영속성 어댑터 안에만 둔다.
- **외부 서버를 타임아웃과 대체 응답 없이 호출하지 않는다.** 상류 장애가 사용자 요청 전체를 실패시키면 안 된다.
- **영속 엔티티를 도메인 패키지 밖에 두지 않는다.** 스캔 대상이 아니라서 **에러 없이 인식되지 않는다** (테스트 픽스처도 마찬가지).
